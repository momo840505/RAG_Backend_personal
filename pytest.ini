# pytest.ini

# Purpose
# -------
# Pytest configuration for Django project. Points tests to the correct settings module
# and makes sure both root 'tests/' and 'api/tests/' are collected.

[pytest]
DJANGO_SETTINGS_MODULE = backend.settings
python_files = tests/**/*.py api/tests/*.py
